# RedHydra OpenCore
# Default public mode needs no API key and no login.

REDHYDRA_MODE=opencore-local-no-key

# Optional only:
# Users may connect their own local Ollama/OpenAI-compatible endpoint from Settings.
# Do not commit real API keys.
